# 实验目录：共享协作入口

适用目录：`/Users/lrc/Documents/研究生资料/实验` 及其子项目。供 DeepSeek Harness、Codex 和后续接手工具共同读取。

## 定位当前任务

- 当前工作目录不等于目标项目。先根据用户任务确定项目，再读取该项目的 `AGENTS.md`；不要改动其他实验。
- `agent-erratum` 的项目入口：`/Users/lrc/Documents/研究生资料/实验/agent-erratum/AGENTS.md`。具体编码分工、服务器操作约束和当前状态路径以该入口为准。
- 不把某项目的模型、数据、预算、部署授权或冻结协议推广到其他项目。用户最新指示优先；共享轻量原则不自动解除项目冻结合同。
- 不依赖 Harness 自动注入本文件。新对话应明确要求读取本文件和目标项目入口；无法读取时如实说明。

## 长期协作方式：先得到可解释的实验结果（2026-09-16）

本节同时约束 Codex 和 DeepSeek。目标是尽快得到可解释的研究结果，不是建设完善的平台。既有任务中的自动审计、评分和封存要求若过重，应由 Codex 结合用户决定明确收缩；不能自行绕过仍有效的冻结协议、删除证据或改写历史结论。

### 每个新对话如何开始

- 先实际读取本文件，再读目标项目的 `AGENTS.md`、该项目指向的当前状态和本次任务文件；旧交付是历史证据，不是当前待办。若路径不可访问，明确说明，不假装已读。
- 开工第一条简短说明：本次目标、最小验收、停止边界。无需复述整份规则或等待重复批准。
- 不假定 Harness 会自动加载 AGENTS.md。Codex 给新对话的任务开头必须附本文件绝对路径并要求先读；DeepSeek 的开工说明用于发现理解偏差，不另建签收文件。

### Codex 的责任

- 实现前给出短任务：目标、允许改动、不可改的实验条件、关键正常路径与已知反例、交付停止点。不把规划遗漏逐轮变成新任务。
- 交付后集中核对影响实际任务、对照和结果解释的部分。新发现只有会破坏这三项时才阻断；假设性加固、通用化和格式完善记录后可推迟。
- 若连续返工主要在新增评估/审计上，先收缩方案，不继续默认扩建；不反复承诺“最后一个阻断点”。
- 负责汇总进展与解释结果，用户不承担两工具之间的技术协调与验收设计。

### DeepSeek 的责任

- 完成最小实现；关键路径要核对真实接口、捕获形状与执行顺序，不能只修到题面反例变绿。检查同一路径的正常情况和直接相邻边界，不扩查无关模块。
- 替身只替必须离线的依赖，明确没验证到哪里；已有真实捕获/SDK/工具应优先复用。测试数量不等于链路可靠。
- 发现新问题先说明其是否影响当前任务。范围内必要修复继续做；会实质改变实验、增加预算或大幅扩建的改动先报告，不自行启动新框架。
- 完成约定验收就停止，只跑新增与直接影响检查；不默认全仓测试或反复重验所有历史交付。失败如实留证，不伪造通过。

### 双方共同的底线与交付

- 保住真实任务与R/E差别、工具/状态更新不丢失、可比条件、原始输入输出和结果证据。工具/模拟器/传输失败与模型能力失败分开。
- 小样本可按两臂一致的规则人工复核；不为替代有限人工检查而自建万能评分器。可选诊断不应拖垮运行，失败仍需可见；不把未达成的有效性条件声称为通过。
- 授权范围内先验证最小真实链路再完整运行；不得擅自联网部署或付费调用，亦不拿昂贵完整实验充当首次集成测试。
- 交付简述“改了什么、验证到哪、剩余什么、能否进入下一步”。明确区分实现完成、离线组件验证、在线任务跑通、结果分析完成；不跨层声称“全链闭合”。
- 跨项目长期规则维护在本文件；项目专属约束维护在项目 `AGENTS.md`；当前进度在项目已有状态入口；任务稿只写本轮差异。用户新指示优先，明确取代旧任务后不继续旧补修。

