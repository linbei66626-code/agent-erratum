# DeepSeek任务：原始t4→t12连续R/E配对，离线实现与验收

## 目标与授权

请在项目 `/Users/lrc/Documents/研究生资料/实验/agent-erratum` 中，实现原数据用户U000828的t4→t12（9个任务）连续R/E配对。用户已选择：先跑原始数据观察erratum效果，不新增历史、不增加偏好修改次数、不删聊天、不做“只交接记忆”的新会话诊断、不增加旧记忆对照。

本轮只授权离线实现、必要测试、PLAN/PREFLIGHT和部署材料准备。禁止SSH、上传、部署、启动Letta/数据库、访问外网、调用真实模型或执行真实RUN。本机隔离的fixture HTTP服务可用于真实传输链测试，须明确标记。完成后停止，由Codex核验后另行部署运行。

先读项目AGENTS.md。本任务与另一个对话中的回放网页实现并行，但不依赖它，不修改其文件；不要重置工作树或覆盖其他对话的改动。

## 研究问题与已有证据

比较连续实际任务中，R改写当前记忆与E保留旧块追加修正的更新和任务表现。低次数双成功不证明长期可靠；任何结果都不预设E更差。本轮不是KV性能实验，不宣称缓存收益、官方benchmark成绩或统计显著性。

已通过的真实单t4配对在：
`transfers/lab-cloud-re-watchdog-run-20260913-r1/`
两臂均正确更新、下单和支付，独立输入审计VALID。保持全部封存字节不变；不能拿封存单t4续跑拼成一轮新配对，也不重放旧副作用。

## 首先核对，不凭交接猜测

重点入口：`ae_inputs.py`、`ae_adapter.py`、`ae_task_run.py`、`ae_vita.py`、`ae_cloud_re_pair.py`、`ae_cloud_re_input_audit.py`、`scripts/ae_01_cloud_re_pair.py`、对应CLI与测试，以及多调用协议、receipt和watchdog。

已读到的边界：
- `ae_inputs.prepare_sample`支持end_turn=12；投影按每任务interactions生成原始历史，检查后续历史日期晚于前一任务日期。不是自行对累积聊天做文本去重。
- `LettaBridge.run_stage`已要求从t4连续推进，维护visible_refs和last_turn。
- `NativeVita`仍限定end_turn=5及subtasks[3:5]；云配对driver仅执行t4。
- 现有原生包装有per-subtask environment isolation，不能把Agent连续性误解成共享同一个订单数据库。

用真实固定数据与固定源码核实上述事实。给出9任务表：任务编号/日期/domain、原instruction、历史条数和日期范围、原始记录摘要、工具数量、评分方式。对t12偏好变化只引用真实记录，不把旧图中的“不另外加糖”直接硬编码成事实。

数据集固定SHA：`3a05f7e2742f204ae28b71db95b14e2daa94c9ff354f510a7d9c707564c7466e`；实验室声明路径 `/root/agent-erratum/vendor/vita/tasks-full-a4553e1.json`。源码路径显式传入，沿用AE_VITA_SOURCE/AE_LETTA_SOURCE约定。旧Mac临时Vita路径在本次检查中已不存在：查找真实现存来源，不造目录、不造源码、不下载、不用skip冒充通过。缺必要来源时完成其余工作并报告具体阻塞。

## 必须保持的实验合同

1. 新运行创建两个独立Agent，均从同一t3正确起点初始化一次。固定顺序为R的t4→t12全部结束，再E的t4→t12；不得交错原生线程注册表。
2. 同臂跨任务保留Agent身份、记忆块、E修正消息、正常聊天及工具交互；不从后续gold偏好重置，不新建空白Agent接任务。逐任务评分transcript与累计Agent上下文分开管理。
3. 每任务只追加该任务原有interactions与instruction，保留原文、日期、顺序及tN/history/i引用。跨任务不重复发送先前批次，不按同值文本删掉合法的不同记录，不发送未来任务。
4. 模型自行判断更新，程序只校验操作合法并按既有MemoryPolicy执行；不替模型填7分糖或任何后续偏好，不以更新正确作为输入VALID的条件。R只有成功更新才PATCH，E不PATCH；E内部账本不成为额外模型可见后端。
5. Vita环境按固定源码和原任务定义初始化。核清原生跨任务用户模拟器推进、工具注册表、domain切换、时间和环境生命周期；显式记录适配，不合并或注入上一任务订单，不改原任务DB，也不把这种环境隔离称作现实世界状态连续。若固定真实语义与已有包装矛盾，先报告证据和阻塞，不自行发明新环境合同。
6. 每阶段只给当前可用工具；历史阶段仅整理记忆。当前工具schema必须来自真实原生环境，禁止沿用上一domain的绑定。工具调用/完整ID/参数/回包严格对应，多调用保持已核顺序协议。
7. 用户回复来自本任务真实原生模拟器；停止消息不转成下一条runtime_user；每任务结束进入原生评分并保留真实环境结果。工具前置错误恢复仍仅限已核分支，不扩大WRITE恢复权限。
8. 原生评分低或任务做错但正常结束，可以继续后续任务并保留失败；不要把它误判成输入无效。评分异常、传输/来源/协议错误、不确定副作用、硬限额或watchdog停止则停止整次配对，保存前面结果，不重试、不跳过、不伪造完整评分。
9. 保留现有模型、temperature、无seed发送、输出上限、65秒节流、65536本地上下文预算及多调用profile。不得为多轮静默扩大上下文或添加总结/压缩提示。检测压缩、裁剪或消息缺失；若触及现有合同边界，记录有界停止，不能声称仍完整保留历史。
10. 请求预算仍为整对、所有角色共享256，不按任务或臂重置，不把256视为本轮付费授权。PLAN需估计18任务的请求与65秒节流耗时，明确这是估计。单t4旧3600秒wrapper不适合直接冒充多轮时限：另给显式总时限候选与依据，标待部署前确认，不修改现有watchdog规则/脚本或后台默认延长。256不足时报告未完成，不能自动加预算。

## 实现范围

优先新增独立多轮driver、审计入口、CLI、候选配置和测试，避免改坏封存单t4协议。允许对 `ae_vita.py`、`ae_task_run.py` 做确有必要的最小显式扩展，默认旧调用行为保持不变；新增参数必须精确校验，不将固定单t4核验器放宽成通用接受器。

不修改上游Vita/Letta、ae_adapter.MemoryPolicy、已有多调用/支付错误证明、代理、启动器、watchdog、旧配置、旧交付、可视化实现。若必须超出上述范围，给出具体文件与原因，停止该项依赖工作，不自行扩大。

开工保存允许修改文件的完整before字节和SHA，交付精确diff。因并行对话存在，明确标注非本任务改动；不要全仓回滚/整理。

## 多轮输入审计

复用已核共享能力，新增独立schema，严格核：
- 固定源码、完整code/provenance清单、显式数据集、multicall manifest、生产RUN前与审计中的共用receipt校验；不跳门、不伪造live receipt。
- 两臂隔离；固定18任务顺序与连续Agent；每阶段进入/结束/评分唯一且顺序正确；阶段开始状态来自上一阶段自己的状态。
- 原历史来源、无未来gold注入、无后续gold重置、累计聊天保留、按臂/任务/phase映射请求响应。
- 原生用户回复逐次来源对应，重复同值合法、乱序/跨任务/跨臂错配拒绝。
- 成功更新与PATCH、bridge帧严格按序一一对应；R同值多次更新合法，E零PATCH；更新正确性与来源完整性分开。
- 辅助请求、响应严格按捕获顺序归属；评估仅对应本任务transcript与环境；所有云记录消费闭合，不能因9任务而按内容全局搜索。
- 所有18任务完成且门通过才给整对VALID；部分完成保留分任务状态，但整对标未完成/INVALID，不把缺失算0分，也不把早先合法低分抹掉。

## 离线验收

1. 真实数据PLAN与PREFLIGHT完全禁网：18阶段、两臂起点相同、9份历史与原文对应、当前工具来源可核、无gold泄漏；PLAN不要求伪造服务receipt，RUN必须在请求前验证真实receipt。
2. 明确标注fixture的完整18任务串行链，走生产CLI/driver、真实Vita包装与各domain工具/schema、真实桥接与CloudAuditProxy、完整审计。只将模型/用户/评分应答替换成可追溯fixture，不mock掉输入/顺序/provenance核验来求PASS。不能做到的覆盖明确列出，不能用纯mock循环冒称真实原生链通过。
3. 新增关键反例：任务跳过/交换/重复；未来历史；第二任务gold重置；同臂Agent更换；E旧修正丢失；旧domain工具误用；跨任务用户回复或评分归属错误；PATCH/辅助乱序；未消费记录；中间任务合法低分仍继续；中间任务评分异常立即停，后续无请求；共享预算不重置；上下文缺失拒绝。
4. 生产新CLI的PLAN/PREFLIGHT/RUN参数和独占输出目录组合通过；已有目录零覆盖；失败保存已发生证据；新入口确实调用生产provenance writer。旧单t4相关定向回归通过，不无理由重复全部历史测试。
5. 新增测试不得靠默认临时目录、导入污染或skip通过；fixture不得标真实能力证据；旧来源摘要不匹配不得用全局digest stub隐藏。

## 交付与停止点

交付新独占目录 `results/ae-cloud-re-multiturn-original-r1/`（已存在则选择新后缀，禁止覆盖），至少含：
- README：实现范围、实际数据核对表、Agent/环境连续性解释、已测与未测边界。
- DELIVERY、before/after完整字节及SHA、精确diff、独占输出示例、测试日志与fixture完整审计报告。
- 单页数据流说明：哪部分跨任务保留，哪部分每任务初始化，模型看到什么，评分看到什么。
- 实验室PLAN/PREFLIGHT/RUN/AUDIT命令，参数与实际CLI一致，各用新目录；上传清单和依赖SHA；网络前置、服务receipt、总预算/时限候选、外层清理与证据回收方案。本轮仅准备，不执行。

不自动写入用户“我的判断”或改历史结论，不宣称多轮已真实跑通。不处理回放网页；完成后给短交付说明并停止。
