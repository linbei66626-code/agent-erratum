# 探索实验收缩执行单（2026-09-16）

开工先读 `/Users/lrc/Documents/研究生资料/实验/agent-erratum/AGENTS.md` 和其中的当前状态入口。第一条回复简述本次目标、最小验收与停止边界，然后继续工作；不要把旧补修交付当作当前待办。

用户已同意停止追求完整自动审计系统。此单取代最近几份继续补修订单时序审计的任务；不要继续实现那些任务。本次不是修更多审计，而是把可选分析从实际实验中拆出来。

## 只做两件事

1. 新建独占的探索候选配置：沿用当前DeepSeek非思考、R/E、t4..t12、角色约束、串行0间隔及既有工具/容量边界，max_requests使用已支持的1024。旧候选不覆盖。代理ops配置也必须1024，继续使用现有预算一致性检查。
2. 在新候选的显式探索选项下，把附加确定性评估改为离线诊断：跳过ae_vita.NativeVita.finish内的deterministic_business_result，以及execute_re_multiturn末尾的separated_evaluation。保留原生judge及其原始输出、每阶段数据库、对话、工具、记忆更新、结束原因。运行结果明确记附加评估未运行/待人工复核，不能伪造通过。旧配置行为不改。

可用一个明确、经校验的配置字段接线，不再创建一整套schema/审计体系。按项目现有配置风格实现；不要为了让旧严格审计接受新探索模式而修改审计器。

允许修改：候选配置、ae_vita.py、ae_cloud_re_multiturn.py、必要的CLI参数传递及少量定向测试。不要修改ae_cloud_re_multiturn_input_audit.py或扩大ae_sim_eval_protocol.py评分功能。

## 保留什么

真实R/E机制、多调用完整接收、禁止摘要、原始证据保存、限额与传输错误停止。保留已有用户角色约束及当前检测；先不再扩检测规则。明显越界仍停并保留原话，不能静默清洗/补答/重试；误拒由原文人工判断，不算Agent能力失败。

已有逐轮状态记录可以原样留下，但不继续开发重建算法，不作为探索分析的硬前置。不删除旧审计，不改旧报告，也不把旧INVALID改成VALID。原生评分链若失败仍如实报告，不能吞掉。

## 只验三件事

- 新候选真实PLAN/PREFLIGHT接受，记录1024，代理预算一致；旧配置不变。
- 实际运行分支确实跳过两处可选计算（将这两个函数设为抛错，探索模式仍能保存阶段/最终结果）；原生judge与raw照常生成。
- 原有传输/工具/原生judge故障仍按原规则停止留证；不能用宽泛try/except吞掉真实实验故障。

只跑对应小套件，不跑全仓，不重验数十历史交付，不部署、不调用模型。不新写大型审计报告。一个新目录交付改动、验收输出、同步清单即可。

## Codex后续执行口径（不是要求DeepSeek实现）

先核现场网络/服务身份与1024预算，再运行已有入口支持的最小真实范围（优先t4单阶段两臂，不另写探针框架）。检查真实工具调用、用户角色及证据落盘；这是冒烟验证，不是研究成绩。通过后另建目录完整两臂运行，绝不拼接冒烟结果。

结果标注“探索运行/人工复核”，不宣称原冻结协议VALID。人工逐阶段记录：原生reward、实际订单与任务要求、用户越界、工具/传输异常、可见新偏好是否被采用及证据。t5/t8争议保留并单列；其余任务也不能因未列入排除就自动可信。模拟器异常与业务未达标分开，未覆盖不等于失败，未付款不自动等于失败。

工作到此收敛，不把下一轮结果分析变成继续建设平台的理由。
