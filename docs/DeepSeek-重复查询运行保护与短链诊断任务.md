# 重复查询：运行保护与短链诊断准备

2026-09-13。用户要求分析并解决。按项目分工待人工转交，未派发。只离线实现和验收；不启动服务、不调用模型、不改旧数据/结果/冻结配置。

## 已核事实

证据 `results/ae-query-loop-diagnosis-20260913-r1/comparison.json` 与 `transfers/lab-cloud-re-payment-run-20260913-r1/`。初始历史请求仅Letta运行元数据不同；current_task正文、工具定义/顺序及实发参数一致。新run历史响应空内容，task中才更新记忆；旧run历史先更新再结束。新R随后门店/商品交替各8次，同参数同回包，后续wire120处回包核对一致；tool_choice一直auto，创建/支付工具可选，provider每次生成不同ID。没有证据证明支付补修导致循环；该分支没有被调用。旧温度0不能被表述为两次响应一定相同的合同，实发请求也不含seed。

已确定：成功查询后的行为循环，以及缺少及时运行止损。尚未确定：模型为什么分叉、何种提示/模型能稳定修复。不要把停止保护称为模型能力修复。

## 允许范围：新增运行保护，不改既有生产

新增 `scripts/deployment/ae_re_query_watchdog.py`、`tests/test_ae_re_query_watchdog.py` 和独占 `results/ae-query-watchdog-20260913-r1/`。不改adapter/driver/audit/提示词/工具/评分/旧配置。本轮仅候选实现，真实启用前须确认新的运行保护规则，不默默改变冻结科学协议。

作为显式启动的外层wrapper，启动一个明确子runner，观察其指定新run events.jsonl，连续候选模式满足时仅停止自己启动的runner，让现有运行脚本清理服务。不得按名字扫描/杀其他进程，不循环重启。只监测本次路径、自己创建的进程；路径已存在拒绝。结果输出为独占sidecar，不能伪造driver result、补评分或把中止改VALID。

候选v1规则：仅任务阶段、同一臂、同一连续交互，get_delivery_store_info / get_delivery_product_info 两个工具的成功结果构成周期1或2，连续3组完全重复。签名含工具名、严格解析并规范化的完整arguments、精确tool_return摘要、block_sha；不含每次变化的call ID，但各ID必须不同且事件不能重复计数。遇任何别的真实动作、用户消息、记忆变化、阶段/臂切换、错误结果、正文/参数变化就重置。不能仅按工具名/调用总次数停。无法确认边界或未完整写完的JSON行不据此触发；已完整的异常JSON应明确报监测失败，不装成成功。

该判定仅是“连续重复输入/输出”的运行止损，不证明全部环境状态相同或模型不可能下一步脱离。新profile侧写清阈值、触发事件范围、签名、时间、runner退出与服务清理结果，统一适用于R/E。主动停止后没有result.json是已知现状，如实标USER/WATCHDOG_STOPPED_DIAGNOSTIC_INCOMPLETE，旧审计不放宽。

先以现有run的只读回放验证周期2第三组结束触发（累计工具结果9，后面还观察到10个重复查询）；这只是历史上的潜在节省，不承诺实时监测精确节省同数。另用无网sleeping子进程及分段写入事件fixture验收停止/不误杀/不重启；全程不发送模型请求。

负例：同名参数变化、正文变化、block变化、READ以外工具介入、真实用户消息/阶段/臂切换、重复旧事件/相同call ID、半行JSON、两组未达阈值均不得触发重复循环停止。原生任意WRITE绝不由watchdog替执行或回滚。明确外层watchdog不是安全沙箱。

## 只准备后续能力诊断，不执行

在交付README给一个有界短链候选：从封存第一次完整重复后的上下文出发，最多3次模型请求，比较原输入与一个独立标记的“先检查已有结果、缺参数则询问、不要重复相同查询”提示候选；保持工具结果真实，不能代填参数/创建订单，不把离线scripted应答当能力证据。只列诊断设计及预算，等待用户取舍后另立真实运行，不编造结果。

这属于改变输入的机制探索，不能混进原R/E比较，不能凭一次脱离循环宣称稳定解决。不得换模型、删历史、调温度、扩大上下文或改任务说明来让测试通过。

交付：新增文件SHA、只读真实轨迹回放、正反例和生命周期日志、简短边界说明。不做全量既有回归，不修改旧交付。Codex之后只核关键误停与进程归属场景。
