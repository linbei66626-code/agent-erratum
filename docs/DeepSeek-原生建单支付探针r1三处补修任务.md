# 原生建单支付探针 r1：三处最小补修

2026-09-13。Codex 已离线复现三个问题；12 项原测试通过不足以关闭它们。按项目分工待用户人工转交，尚未派发。本轮只离线补修，不部署/联网/RUN。此前最多 3 次真实模型请求的授权保持，修后核验通过再执行。

## 范围

只改 `scripts/ae_01_native_order_probe.py`、`tests/test_ae_native_order_probe.py`，新增独占 `results/ae-native-order-probe-20260913-r2/`。修改前完整字节冻结并核 SHA；r1 与 Codex 核验目录保持原样。不改其他生产代码、工具、模型、提示词、案例、预算、评分或旧 raw。

## 三处修复与验收

1. **不能在一批调用中途宣布成功而吞掉尾部。** 当前第一请求创建；第二请求返回 pay(真实旧 ID) + create 时，支付后立即 return PASSED，第二个 create 被丢弃。按 provider 原顺序处理整批，只有整批结束且无未处理调用后检查最终订单状态并决定通过；副作用不确定仍立即停止且不算通过。成功后立即停指不再发下一模型请求，不指跳过已收到的调用。此反例应有完整执行/回包证据，最终两张订单不得通过；补支付后跟未声明/非法调用等反例，不得以首条成功掩盖尾部。正常两请求链仍通过，不增加请求预算。

2. **规格核对不能是子串搜索。** 当前真实订单 attributes 为“不要7分糖，改5分糖”仍通过。对冻结案例明确采用规范值 `规格: 7分糖` 的完整匹配（原生返回的结构按实际表示核对），不要用含有“7分糖”证明正确，不用自由文本猜测器，不改模型输入/实际工具参数。保留真实错误订单并标未完成；新增否定句、5分糖、17分糖等反例与规范值正例。解释精确匹配的适用范围：这是冻结小案例，不能推广为任意自然语言糖度判定。

3. **拒绝旧目录必须零写入。** 当前已有目录 keep 被拒绝 exit 2，但 main 的异常处理又新增 result.json。仅在本次调用确实成功独占创建输出目录后，才能在异常路径写失败报告；创建失败或输出目录已有时不得写进去。补真实 CLI 测试：已有空/非空目录、已有 result.json，前后目录清单与字节一致；本次拥有的新目录发生后续错误仍能保存有界失败结果，不覆盖已有文件。

## 证据与交付

复现入口 `results/ae-native-order-probe-codex-review-20260913-r1/probe.py`，结果 `probe.json`。使用真实 Vita 与真实 CloudAuditProxy、明确 fixture；原用例及新增反例定向回归即可，不扩全项目回归。交付完整 before/after 字节与 SHA、逐行 diff、真实测试日志及复现结果。不要修改原探针以删除反例；可另写修后验收脚本，说明每个状态变化。无新真实云能力证据，scientific_result 仍 null。
