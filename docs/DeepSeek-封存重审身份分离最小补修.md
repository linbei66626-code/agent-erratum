# 决定：采用审计者身份分离，只补封存重审入口

选(a)，不做一次性豁免。问题是旧plan把事后审计模块也收入16个代码摘要，新审计因此无法复查旧实验；不代表实验需要重跑。

## 最小范围

保留默认严格审计行为、旧plan/result/代码列表不变；新增显式离线重审选项。对本次唯一发生变化的 `ae_cloud_re_multiturn_input_audit.py`，必须提供运行时旧文件的真实封存副本，重新计算其SHA并与plan/result中旧摘要逐一相等。其他运行身份文件仍逐项严格核对，不允许任意传入“忽略文件列表”。缺旧副本/摘要不符/plan与result不一致一律拒绝。

将本次实际执行的新审计者身份单独写入新报告：入口、multiturn审计模块、基础ae_cloud_input_audit.py及直接使用的审计支持模块的路径和真实SHA，记录重审模式与旧/新审计摘要。不得拿旧代码哈希冒充当前执行代码；不能从provenance集合直接删除审计文件后宣布通过。

优先用独立审计目录或显式旧审计文件参数实现，不改运行驱动、不改已有MULTITURN_CODE_FILES、不重建stack、不重启服务。无需搭建通用归档或版本管理系统。本次旧审计文件应能从budget1024交付after/或现场未替换代码取得，必须实算7d631dfb8a3e13a22619ae78b64078a538720d47b51adc8473e1ec5b1868f4f6，禁止重构“相似旧文件”。

## 定向验收与停止点

1. 默认入口遇到审计代码变化仍拒绝。
2. 显式重审+旧文件摘要匹配通过身份门；运行文件被改、旧审计缺失/错误、plan/result摘要不一致仍拒绝。
3. 新报告包含真实新审计代码身份，不能只写一个通过标记。
4. 保留上一轮ID映射反例，不放宽其它内容审计。
5. 列出现场部署/重审命令，dataset用绝对路径，新报告绝不覆盖旧报告。当地stack路径不能通过修改封存plan解决；现场具有原清单与回执，用现场离线重审确认后续门。

只跑相关定向测试，不跑全仓，不调用模型、不联网、不SSH、不重跑R/E。新独占交付。若出现下一处独立缺陷，定位证据后报告，不自行继续扩大。

当前仍不能声称ID修复后的完整审计通过：该轮公开入口只到load。此次完成后由Codex在现场旧证据上运行完整新审计，才报告最终状态。
