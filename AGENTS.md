# 项目协作入口

## 固定编码分工（用户要求，2026-09-11）

为节省 Codex 额度，本项目默认由 **DeepSeek Harness 编写、修改代码及编写测试**；Codex 负责研究讨论、明确任务与边界、检查交付、必要验证及解释结果。不要让 Codex 和 DeepSeek 重复实现同一项任务。

- Harness 入口为用户已提供的 `http://127.0.0.1:3080/`；先核当前服务和会话，不能假装已经委派或完成。
- Codex 给出简短、具体的任务：允许修改的文件、不能改变的实验定义、验收要求。优先让 DeepSeek 在授权范围内完成实现与测试，Codex 按风险核对关键 diff、真实输入和证据，不重复全量劳动。
- Harness 不可用、受阻或交付失败时，向用户说明；**不得默默改成 Codex 自行完成大段编码**。转由 Codex 编码需用户明确同意。文档更新、只读检查和已授权部署操作不属于这一编码限制。
- 委派不扩大权限：不自动改冻结实验定义／阈值、不覆盖旧 raw、不自动扩展付费实验；不把真实 API key／密码放进委派提示词、对话或交付包。
- 用户在服务器录入密钥后的通知仅说明用户已操作；助手若尚未检查，不声称密钥验证通过。

## 共享协作规则（2026-09-16）

先读父目录共享入口：`/Users/lrc/Documents/研究生资料/实验/AGENTS.md`。
长期的结果优先、最小验收、双方责任、停止扩建和交付口径在父入口统一维护，不在此重复。本文件保留本项目专属分工、网络前置和状态入口；轻量协作不解除本项目仍有效的冻结实验合同。

## 实验室运行前的网络检查（用户要求，2026-09-13）

- 校园网认证会过期，SSH 可登录不代表服务器能出网。每次实验室实际实验前、长时间暂停后恢复时，先从目标服务器检查云 API 的 HTTPS 连通性，再启动实验服务或发送推理请求。
- 检查不携带 API Key/校园网凭据，不调用模型；例如对 `https://api.siliconflow.cn/v1/models` 做有短超时的无鉴权 GET。401/403 可证明 HTTPS 可达，但不证明 API Key 或模型可用。保存时间、状态码或错误类型。
- 无 HTTP 响应/连接超时则停止在联网前置步骤，先请用户重连校园网并复核；不要让正式 RUN 充当网络探针，不自动重试模型或修改冻结超时。校园网密码由用户交互输入，不写日志或交付包。

## 恢复进度

- 知识库当前状态：`/Users/lrc/Documents/Obsidian Vault/论文阅读/80-实验与复现/Agent场景多轮修正/Agent场景多轮修正-当前状态.md`。
- 部署入口：`docs/lab-migration.md`；云任务适配：`docs/cloud-task-compat.md`。
- 当前实施分工不改写历史 provenance：此前由 Codex 实现的代码仍如实记录为 Codex 实现。
